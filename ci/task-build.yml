platform: linux
image_resource:
  type: docker-image
  source:
    repository: node
    tag: alpine
run:
  path: npm
  args:
    - run
    - build
  dir: resource-code
inputs:
  - name: resource-code
